// Debug test to see what's available
println("Available builtin functions:")

// Test a few known builtins first
println("print exists:", type(print))
println("len exists:", type(len))
println("chr exists:", type(chr))

// Now test for debounce
// If this fails, we'll know it's not registered
println("About to test debounce...")
println("debounce type:", type(debounce))