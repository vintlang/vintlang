// Simple enum example

enum Status {
    PENDING = 0,
    ACTIVE = 1,
    COMPLETED = 2
}

let current = Status.ACTIVE
print(current)

// String enum
enum Color {
    RED = "red",
    GREEN = "green",
    BLUE = "blue"
}

let myColor = Color.RED
print(myColor)

