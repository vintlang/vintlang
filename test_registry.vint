// Test to check if builtin functions are properly registered
println("Checking builtin registry...")

// Try to check if the builtins are accessible from the evaluator
// This is just to test if the registry is working

println("Testing basic builtins...")
println("Type of 'print':", type(print))
println("Type of 'len':", type(len))

// The debounce function should be available now
println("Testing debounce availability...")