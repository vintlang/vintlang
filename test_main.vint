// Test main function detection
import time

// This will be executed during the first pass
println("Setting up the program...")

// Define a helper function
let greet = func(name) {
    println("Hello,", name)
}

// Define the main function
let main = func() {
    println("Inside main function!")
    greet("VintLang")
    println("Current time:", time.now())
    return "Main completed successfully"
}

// This will also be executed during first pass
println("Program setup complete.")