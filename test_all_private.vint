include "./examples/packages_example/enhanced_test.vint"

// Test accessing all the private members to see which ones are blocked
debug("Testing _privateSecret...")
// This should be blocked:
// debug(enhanced_test._privateSecret)

debug("Testing _PRIVATE_KEY...")  
// This should be blocked:
// debug(enhanced_test._PRIVATE_KEY)

debug("Testing _validateInput function...")
// This should be blocked but isn't:
debug(enhanced_test._validateInput("test"))

debug("Testing _logDebug function...")
debug(enhanced_test._logDebug("test message"))

debug("Testing _getPrivateKey function...")
debug(enhanced_test._getPrivateKey())